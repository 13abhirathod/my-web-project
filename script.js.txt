// script.js

// Welcome Message on Load
document.addEventListener('DOMContentLoaded', function() {
    console.log("%cWelcome to My First Website! 🚀", "color: #667eea; font-size: 16px; font-weight: bold;");

    // Button Click Effect
    const btn = document.querySelector('.btn');
    if (btn) {
        btn.addEventListener('click', function() {
            alert("🎉 Thank you for clicking! You're doing great!");
            
            // Button animation on click
            this.style.transform = "scale(0.95)";
            setTimeout(() => {
                this.style.transform = "scale(1)";
            }, 150);
        });
    }

    // Live Clock
    function updateClock() {
        const now = new Date();
        const timeString = now.toLocaleTimeString('hi-IN', { 
            hour: '2-digit', 
            minute: '2-digit', 
            second: '2-digit' 
        });
        
        // Agar aap clock show karna chahte ho to index.html mein ek <p id="clock"></p> add karke use kar sakte ho
        const clockElement = document.getElementById('clock');
        if (clockElement) {
            clockElement.textContent = "🕒 " + timeString;
        }
    }

    // Update clock every second
    setInterval(updateClock, 1000);
    updateClock();

    // Dark Mode Toggle
    const toggleDarkMode = () => {
        document.body.classList.toggle('dark-mode');
        
        if (document.body.classList.contains('dark-mode')) {
